import { useEffect } from "react";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { supabase } from "@/lib/supabase";
import { useAuth } from "@/contexts/authContext";*** End Patch  to=functions.apply_patch (json error) code? need proper format. We must follow grammar. Let's craft entire file carefully. We'll reattempt. Need ensure path to `useAuth` correct case `@/contexts/AuthContext`. Let's craft full file content. We'll use apply_patch add file. We'll include TypeScript definitions etc.

